import {StyleSheet} from 'react-native';
import {spacings} from '../../constants/spacings';

export const styles = StyleSheet.create({
  body: {marginHorizontal: spacings.s2},
  scroll: {
    // paddingStart: spacings.s5,
  },
});
